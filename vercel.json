{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install && npm install --prefix site",
  "buildCommand": "npm --prefix site run build",
  "outputDirectory": "site/dist",
  "framework": "vite"
}
