{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install && npm install --prefix site",
  "buildCommand": "node pipeline/build_data.js || echo '[build] pipeline failed; using committed travel_data.json'; npm --prefix site run build",
  "outputDirectory": "site/dist",
  "framework": "vite"
}
